services:
  nextjs-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: portfolio:dev
    container_name: portfolio-dev
    ports:
      - "3000:3000"
    environment:
      - WATCHPACK_POLLING=true
    develop:
      watch:
        - action: sync
          path: .
          target: /app
          ignore:
            - node_modules/
            - .next/
        - action: rebuild
          path: package.json